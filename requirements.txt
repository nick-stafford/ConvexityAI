# Convexity AI - Dependencies (Streamlit Community Cloud)

# UI
streamlit>=1.30.0
plotly>=5.18.0

# Data
yfinance>=0.2.36
pandas>=2.0.0
numpy>=1.24.0

# AI
groq>=0.4.0
google-generativeai>=0.3.0  # optional: image generation, only active if GEMINI_API_KEY is set

# Utilities
python-dotenv>=1.0.0
requests>=2.31.0
beautifulsoup4>=4.12.0
schedule>=1.2.0

# NOTE: sentence-transformers, lancedb, and stitch-ai were removed.
# They pulled in torch (~2GB) and exceeded the free-tier build, and nothing
# in the codebase actually imports them.
